name: Windows

on:
  push:
    branches: [ v3.1-mono ]


jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.201
    - name: Display dotnet info
      run: dotnet --info
    - name: Install Chocolatey packages
      run: choco install xunit opencover.portable codecov
    - name: Build all projects
      run: dotnet build
    - name: Run unit tests
      run: OpenCover.Console.exe -register:user -target:"C:/Program Files/dotnet/dotnet.exe" -targetargs:test -filter:"+[KonSchool*]* -[KonSchool.Tests*]*" -output:".\KonSchool_coverage.xml" -oldstyle
    - name: Upload coverage to Codecov
      run: codecov -f "KonSchool_coverage.xml" -t ${{ secrets.CODECOV_UPLOAD_KEY }}
